apiVersion: v1
kind: ConfigMap
metadata:
  name: scaler-config
  namespace: autoscale-test
data:
  config.yaml: |
    web_ui_port: 8085
    slo: 500
    scaling_namespace: "microservices-demo"
    prometheus_url: "http://prometheus.monitoring.svc.cluster.local:9090"
    analyzer_interval: 15s
    scaling_cooldown: 150s
    lambda: 0.6
    anomaly_services_count: 3
    logger:
      level: "info"
      format: "text"
