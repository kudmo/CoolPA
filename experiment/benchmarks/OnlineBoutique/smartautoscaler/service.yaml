apiVersion: v1
kind: Service
metadata:
  name: smartautoscaler-web
  namespace: autoscale-test
  labels:
    app: smartautoscaler
spec:
  type: ClusterIP
  selector:
    app: smartautoscaler
  ports:
    - port: 8085
      targetPort: 8085 